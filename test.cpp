#include <bits/stdc++.h>
using namespace std;

#define FOR(i, N) for (int i = 0; i < N; i++)
#define FOR(i, a1, a2) for (int i = a1; i < a2; i++)
#define FORIT(it, A) for (auto it = A.begin(); it != A.end(); it++)
#define PRINTARR(A, N) FOR(i, N) cout << A[i] << ' '; cout << '\n';
#define CINARR(cin, A, N) FOR(i, N) cin >> A[i];
#define PQ priority_queue
#define IP pair<int, int>
#define VI vector<int>
#define LL long long
#define PB push_back
#define MP make_pair
#define INF 1 << 30
#define LINF 1LL << 60

ofstream fout (".out");
ifstream fin (".in");

int main() {
  
}
